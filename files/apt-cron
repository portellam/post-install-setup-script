# /etc/cron.d/apt-cron:    crontab entries to upgrade software packages
#
# Author(s):    Alex Portell <github.com/portellam>
#

# apt #
#
# auto-upgrade every 8 hours
0 0,8,16 * * * apt clean && apt update && apt full-upgrade -y
#
# auto-upgrade and autoremove every 8 hours
#0 0,8,16 * * * apt clean && apt update && apt full-upgrade -y && apt autoremove -y
#